# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com

# Flask Configuration
FLASK_SECRET_KEY=your-secure-random-secret-key-here

# Database Configuration (optional - defaults to SQLite in instance/ folder)
# DATABASE_URL=sqlite:///instance/user_data.db

# Environment Configuration (optional - defaults to development)
# FLASK_ENV=development

# Instructions:
# 1. Copy this file to .env for local development
# 2. Replace the placeholder values with your actual configuration
# 3. Generate a secure secret key with: python -c "import secrets; print(secrets.token_hex(32))"
#
# For cPanel Production Deployment:
# - DO NOT upload the .env file to production
# - Instead, use cPanel's "Setup Python App" -> Environment Variables section
# - Set FLASK_ENV=production in cPanel to disable .env loading
